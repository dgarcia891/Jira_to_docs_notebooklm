# Core Engineering Standards (v6.3)
1.  **The 500-Line Limit:** Files > 500 lines are **FORBIDDEN**. Refactor immediately [Source 474].
2.  **Test-First Mandate:** You cannot fix a bug without a FAILING test in `tests/regression/`.
3.  **Active Recall:** Run `node scripts/consult.js` before coding [Source 585].
4.  **No Vibe Coding:** Check `docs/architecture.md` first.
